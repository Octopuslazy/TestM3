// Entry point expected by playable-scripts
// Import generated embedded assets so builds include all assets as data-URIs
import './embedded-assets';
// Re-export or import your existing main entry so the build tool finds it.
import './main';
